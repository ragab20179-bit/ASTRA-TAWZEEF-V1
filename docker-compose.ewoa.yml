version: '3.8'

services:
  k6:
    image: grafana/k6:latest
    networks:
      - default
    volumes:
      - ./tests/ewoa:/tests

networks:
  default:
    name: astra-tawzeef-v1_default
    external: true
